import { ProwadzacyContainer } from './prowadzacy-container';

describe('ProwadzacyContainer', () => {
  it('should create an instance', () => {
    expect(new ProwadzacyContainer()).toBeTruthy();
  });
});
